import 'dart:ui';

abstract final class AppColors {
  // Background
  static const backgroundDark = Color(0xFF1A1A2E);
  static const backgroundMedium = Color(0xFF16213E);
  static const backgroundLight = Color(0xFF0F3460);

  // Surface
  static const surfaceCard = Color(0xFF1E2A4A);
  static const surfaceOverlay = Color(0x33FFFFFF);
  static const surfaceChip = Color(0x26FFFFFF);

  // Primary
  static const primaryBlue = Color(0xFF4361EE);
  static const primaryBlueLight = Color(0xFF5A7BFF);

  // Accent
  static const accentGold = Color(0xFFDAA520);
  static const accentGoldLight = Color(0xFFFFD700);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0C3);
  static const textTertiary = Color(0xFF7A7A8E);

  // Tags
  static const tagWisdom = Color(0xFF2D6A4F);
  static const tagCourage = Color(0xFF9B2226);
  static const tagPirate = Color(0xFF3A5A8C);

  // Misc
  static const divider = Color(0x1AFFFFFF);
  static const locked = Color(0xFF4A4A5E);
  static const iconDefault = Color(0xFFB0B0C3);
  static const shadowDark = Color(0x40000000);
  static const transparent = Color(0x00000000);
}
